# build:
  stage: build
  script:
    - npm install
    - npm run build
# test:
  stage: test
  script:
    - npm test
# deploy:
  stage: deploy
  # script:
  #   - scp -r build user@server:/var/www/my-app